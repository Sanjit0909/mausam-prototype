ENV=development

# Google Gemini (AI Weather Assistant). Leave blank to use the rule-based fallback assistant.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash

# Allowed frontend origin(s) for CORS
CORS_ORIGINS=http://localhost:3000
